
``` js
function sumAll(arr) {
  var result = 0;
  arr.sort(function(a,b) {
    return a - b;
  });
  var start = arr[0];
  var end = arr[1];
  
  for (var i = start; i <= end; i++) {
    result += i;
  }
  return result;
}

sumAll([1, 4]);
```
